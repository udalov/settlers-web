#main
  #header
    include inc/header
  #content
    h2 Submit
    hr
    br
    | Submit your solution (single .java file):
    form(enctype="multipart/form-data", method="post")
      input(type="file", name="file")
      input(type="submit")
    br
    | Submissions:
    br
    table#submissions
      tr
        th Time
        th Status
      for submission in submissions
        tr
          td: a(href="/submission/" + submission._id) #{strftime(submission.date, "%d.%m.%g %H:%M:%S")}
          if (submission.status == 1)
            td.compiling compiling...
          else if (submission.status == 2)
            td.error
              | error&nbsp;
              a(href="/compilation-error/" + submission._id) (?)
          else if (submission.status == 3)
            td.ok ok
          else
            td.waiting waiting
    br

  #footer

